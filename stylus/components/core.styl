// Core 

// placeholder para sass-container
$container
  display: flex
  flex-wrap: wrap
  max-width: $max-width
  margin-left: auto
  margin-right: auto
  
// mixin para crear un container
container()
    @extend $container

// mixin para ocupar todo el ancho
full()
  $max-width: 100%